apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitlab-arm-chart.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "gitlab-arm-chart.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitlab-arm-chart.fullname" . }}']
  restartPolicy: Never
