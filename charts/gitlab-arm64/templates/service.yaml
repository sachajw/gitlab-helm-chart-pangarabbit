apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitlab-arm64-pangarabbit.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: {{ include "gitlab-arm64-pangarabbit.name" . }}
