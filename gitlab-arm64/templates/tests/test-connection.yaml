apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitlab-arm64.fullname" . }}-test-connection"
  labels:
    {{- include "gitlab-arm64.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitlab-arm64.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
